# P11 前端重构验收

## P11.1 工作台与地图

- 日期：2026-09-08。
- 基线：`db18966f4f9091f3c153867aec08977395d9a25c`；实施前实时远端 main 与本地一致。
- 变更：浅色任务工作台、地图默认首页、四分区导航、授权对话框、数据源异常排序与详情折叠、地图尺寸刷新与底图异常提示。
- 精确候选 tree：`c4d1e21276be986de1e227be3a4569b244363a71`；腾讯隔离目录重建 Git 索引后逐字核对一致。
- 远端门禁：`go mod verify`、`go test ./... -count=1`、`go vet ./...`、`go build` 全部通过；其中 scripts 的候选敏感信息扫描测试通过。
- Chromium：原风险地图 26 项与新增工作区 7 项，共 33/33 通过，无跳过。覆盖导航历史、输入保留、授权内存边界、底图失败与四个屏幕宽度。
- 截图：验证镜像补充中文字体后完成地图首屏复核；截图使用测试夹具及底图失败场景，不代表实时供应商验收。
- 隔离目录：`/tmp/ai-gdm-ui-20260908`。未升级生产、未变更数据库和缓存；既有 PROGRESS 运维记录不夹带提交。
- 本文是候选验证后的非运行时记录；P11.2 补齐疏散、评估布局与完整业务浏览器验收。

## P11.2 疏散与评估

- 状态：2026-09-08 完成。
- 变更：疏散表单、候选结果与路线选择高亮；评估结果优先布局、高级快照折叠、审计展开、结果直达 AI、窄屏金额纵向排版。导航不清空状态，后台过期规则不因面板隐藏而暂停。
- 接口：后端 API、确定性规则、鉴权协议与风险快照事件契约不变。新增的工作区可见事件仅用于地图尺寸刷新；图标来自本地 Lucide 0.468.0，保留其许可证。
- 精确运行候选 tree：`1e65963b6d70b9239bee0152a504e0436776b905`。
- Git archive SHA-256：`bc3a8f8e38594bded4c36717590ddd2663b54ff6c638233e6943a300361bf0eb`；腾讯隔离目录重建 Git 索引后 tree 一致。
- Go：该候选的 `go mod verify`、`go test ./... -count=1`、`go vet ./...`、`go build` 全部通过，包含候选敏感信息扫描；未连接生产数据库。
- 浏览器：`scripts/validate-workspace-browser.sh` 执行四组真实 Chromium 回归。风险与导航 34、疏散 43、评估 127、安全 5，共 209/209；失败、跳过、不稳定项均为零。安全 JSON 报告另经原有 expected-tests 清单审计通过。
- 视觉：1440×900、1920×1080、768×1024、390×844 共 16 张截图；本地静态图标完整、页面无横向溢出，风险与路线在四个尺寸共 8 次画布像素检查均有实际绘制。桌面、平板与手机人工截图复核未发现文字遮挡或控件溢出。
- 底图边界：腾讯环境访问 OpenStreetMap 的 HTTPS 请求及显式 IPv4 请求均超时；8 次真实底图检查均记录 unavailable，而非 loaded。验证的是超时提示、独立业务状态及风险/路线几何绘制，不宣称真实底图正常或供应商业务验收完成。未擅自切换地图供应商。
- 隔离预览：仅监听服务器 loopback，通过本机 SSH 隧道的 `http://127.0.0.1:18780` 访问。临时代理仅合并三组测试夹具并对齐夹具快照标识；页面明确标识非实时数据，代理不访问生产服务。预览跨分区完成风险自动绑定、灾损、AI 解读和设施搜索烟测。
- 运行隔离：开始与结束的生产及其他项目容器 ID、名称、镜像排序指纹逐字一致；生产 `/readyz` 返回 ready。验证后根分区剩余约 5.8 GiB；保留预览进程和专用测试缓存，未清理共享缓存。
- 制品：远端 `/tmp/ai-gdm-ui-20260908/evidence`；本机 `.tools/frontend-evidence/screenshots-delivery`。截图、JSON 报告、临时预览代理、密钥和运行数据不进入 Git。
- 提交边界：最终提交仅在上述验证 tree 之后增加本文件及 P11 进度记录，不改变受测运行代码；既有运维记录和其他线程文档继续保留为未提交变更。不部署生产。
