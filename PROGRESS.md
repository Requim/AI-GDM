# AI-GDM 开发进度

## 固定信息

- 远端仓库：`https://github.com/Requim/AI-GDM.git`
- 主分支：`main`
- 当前交付：面试可运行 MVP
- 首发灾种：滑坡、泥石流
- 后端语言：Go 1.26.7
- 数据口径：动态数据实时获取，基础数据使用最新可追溯版本
- 前端边界：Go 服务端模板，地图和局部交互使用最少量 JavaScript
- 安全规则：密钥、原始数据、运行数据、二进制和 Docker 镜像不进入 Git
- 发布规则：每个小阶段必须测试、提交、推送并校验 `origin/main`
- 验证环境：容器、数据库、缓存、完整构建和浏览器验收在指定腾讯 Ubuntu 服务器执行；连接参数不进入 Git
- 本地边界：只进行源码编辑、格式化和快速静态检查，不启动项目验证依赖

## 当前快照

| 项目 | 状态 |
| --- | --- |
| 当前阶段 | P10.2 二进制与原始镜像进行中 |
| 最近完成 | P10.1 容器部署 |
| 下一步 | 建立 Linux/Windows 二进制、完整 Docker 原始镜像和校验清单打包流程 |
| 阻塞项 | 无；未导入真实已批准基线时损失评估按数据不足降级 |
| 最后更新 | 2026-08-30 |

## 总体清单

| 阶段 | 状态 | 目标提交 |
| --- | --- | --- |
| P0.1 仓库初始化 | 已完成 | `chore(repo): 初始化仓库与进度追踪机制` |
| P0.2 Go 工具链 | 已完成 | `chore(toolchain): 初始化 Go 工具链与持续集成` |
| P0.3 CodeGraph 与架构约束 | 已完成 | `chore(codegraph): 初始化代码索引与架构约束` |
| P1.1 服务基础 | 已完成 | `feat(server): 建立 HTTP 服务与健康检查` |
| P1.2 领域与端口 | 已完成 | `feat(domain): 建立灾害辅助决策领域模型` |
| P1.3 数据基础设施 | 已完成 | `feat(storage): 建立空间数据库与缓存基础设施` |
| P2.1 外部调用基座 | 已完成 | `feat(provenance): 建立外部数据调用与溯源机制` |
| P2.2 LHASA 接入 | 已完成 | `feat(lhasa): 接入近实时滑坡风险数据` |
| P2.3 栅格处理 | 已完成 | `feat(raster): 实现滑坡风险栅格空间处理` |
| P2.4 气象与调度 | 已完成 | `feat(weather): 接入实时气象数据与采集调度` |
| P2.4.1 LHASA 定时采集与回退 | 已完成 | `fix(lhasa): 补齐定时采集与最后成功数据回退` |
| P2.4.2 NASA Earthdata GIS 数据源 | 已完成 | `fix(lhasa): 切换至 Earthdata GIS 实时栅格服务` |
| P3.1 风险引擎 | 已完成 | `feat(risk): 实现滑坡泥石流风险分级引擎` |
| P3.2 空间分析 | 已完成 | `feat(spatial): 实现风险区域空间分析` |
| P3.3 预警接口与扩展机制 | 已完成 | `feat(hazard): 提供风险预警接口与多灾种扩展机制` |
| P4.1 地图安全与坐标 | 已完成 | `feat(map): 建立地图安全代理与坐标转换` |
| P4.2 避险设施 | 已完成 | `feat(shelter): 实现避险设施搜索与筛选` |
| P4.3 路线规划 | 已完成 | `feat(route): 接入多方式疏散路线规划` |
| P4.4 路线安全评分 | 已完成 | `feat(evacuation): 实现疏散路线安全评估` |
| P5.1 基线数据 | 已完成 | `feat(baseline): 建立损失评估基线数据` |
| P5.2 损失引擎 | 已完成 | `feat(loss): 实现可解释灾害损失估算` |
| P5.3 损失接口 | 已完成 | `feat(loss-api): 提供灾害损失评估接口` |
| P6.1 历史案例 | 已完成 | `feat(case): 建立失联人员历史案例库` |
| P6.2 生还评估 | 已完成 | `feat(survival): 实现生还可能性辅助评估` |
| P6.3 回放接口与模型卡 | 已完成 | `feat(replay): 提供历史案例回放与模型说明` |
| P7.1 搜索证据 | 已完成 | `feat(search): 接入实时灾害信息搜索` |
| P7.2 OpenAI 兼容 LLM 客户端 | 已完成 | `feat(llm): 接入受约束的大模型报告生成` |
| P7.3 AI 编排 | 已完成 | `feat(agent): 建立可审计的智能研判流程` |
| P8.1 控制台框架 | 已完成 | `feat(web): 建立灾害监控控制台` |
| P8.2 风险地图 | 已完成 | `feat(map-ui): 实现实时风险地图交互` |
| P8.2.1 Docker 验证所有权兼容 | 已完成 | `fix(validation): 兼容非 root 工作区的容器校验` |
| P8.2.2 风险地图时效与负载边界 | 已完成 | `fix(map-ui): 修复风险图层时效与负载边界` |
| P8.2.3 风险地图生产契约边界 | 已完成 | `fix(map-ui): 收紧风险地图生产契约边界` |
| P8.2.4 全省略图层到期状态 | 已完成 | `fix(map-ui): 修复全省略图层到期状态` |
| P8.3 疏散工作台 | 已完成 | `feat(evacuation-ui): 实现疏散调度工作台` |
| P8.4 评估界面 | 已完成 | `feat(assessment-ui): 完成智能评估与报告界面` |
| P9.1 安全加固 | 已完成 | `feat(security): 加固评估服务安全边界` |
| P9.2 可观测与降级 | 已完成 | `feat(observability): 完善监控审计与服务降级` |
| P9.3 完整测试 | 已完成 | `test(system): 完成系统级自动化验证` |
| P10.1 容器部署 | 已完成 | `build(docker): 完成生产容器与编排配置` |
| P10.2 二进制与原始镜像 | 进行中 | `build(package): 建立二进制与离线镜像打包流程` |
| P10.3 一键部署验收 | 未开始 | `test(deploy): 验证离线一键部署流程` |
| P10.4 正式交付 | 未开始 | `chore(release): 完成面试评估版本交付` |

## 阶段记录

### P0.1 仓库初始化

- 状态：已完成
- 目标：建立 UTF-8、忽略规则、进度追踪和远端主分支。
- 验证：`git diff --check` 通过；敏感信息扫描无命中；远端 SHA 在推送后校验。
- 已知风险：无；若首次推送认证失败则停止进入 P0.2。

### P0.2 Go 工具链

- 状态：已完成
- 目标：建立 Go 1.26.7 模块、跨平台检查脚本和持续集成。
- 验证：官方发行包 SHA-256 校验通过；`scripts/check.ps1` 完成格式、构建、测试和 vet 检查。
- 已知风险：系统未安装全局 Go，本地使用被 Git 忽略的官方便携工具链；CI 使用固定 Go 1.26.7。
- 修正记录：首次格式检查误扫 `.tools/go`，已限制为项目 Go 文件并复验通过。

### P0.3 CodeGraph 与架构约束

- 状态：已完成
- 目标：初始化 CodeGraph，固化 SOLID、目录依赖和阶段提交规则。
- 验证：CodeGraph 已索引 3 个文件、8 个节点和 10 条边；架构文档和项目指令覆盖核心依赖方向。
- 已知风险：CodeGraph 本地数据库不进入 Git，需要每个新工作区单独执行初始化。

### P1.1 服务基础

- 状态：已完成
- 目标：实现环境配置、结构化日志、请求 ID、健康探针和优雅停机。
- 验证：`scripts/check.ps1` 通过；覆盖默认配置、错误配置、JSON 日志、健康状态和请求 ID。
- 已知风险：本阶段只建立进程就绪状态，数据库和供应商就绪检查在对应阶段接入。

### P1.2 领域与端口

- 状态：已完成
- 目标：定义数据溯源、灾害、空间、疏散、损失、历史事件、搜救辅助和报告模型，并拆分小粒度端口。
- 验证：`scripts/check.ps1` 通过；覆盖数据有效期、坐标、概率阈值、路线方式、成本情景带和合成场景边界。
- 已知风险：领域模型只固定 MVP 必需字段，供应商特有字段必须留在适配器 DTO 内。

### P1.3 数据基础设施

- 状态：已完成
- 目标：建立 PostGIS、Redis、嵌入式迁移和灾害快照仓储适配器，并在组合根激活运行配置。
- 验证：腾讯 Ubuntu 验证服务器执行 `sudo sh scripts/validate-storage.sh`；远端 `go test/go vet/go build` 通过，PostGIS 3.5.2 迁移与空间往返通过，Redis 返回 `PONG`。
- 已知风险：验证脚本当前使用上游可变镜像标签，P10 发布阶段必须锁定构建时解析出的镜像摘要。

### P2.1 外部调用基座

- 状态：已完成
- 目标：建立 HTTPS、限流、重试、缓存、响应上限、敏感参数脱敏、流式下载、原子文件和 SHA-256 溯源机制。
- 验证：腾讯 Ubuntu 服务器执行 `sudo sh scripts/validate-go.sh`，远端 `go test/go vet/go build` 通过；同步脚本完成仅源码原子替换验证。
- 已知风险：各供应商的业务错误码和限额不同，必须在具体适配器中追加契约判断，不能只依赖 HTTP 状态。

### P2.2 LHASA 接入

- 状态：已完成
- 目标：动态解析 NASA LHASA 2.1.1 近实时 GeoTIFF 目录，发现最新时间戳文件并通过通用下载器保存原始制品。
- 验证：目录 HTML 契约测试覆盖最新文件、重复链接、跨主机链接、过期判定和无候选错误；腾讯 Ubuntu 服务器执行 `sudo sh scripts/validate-go.sh`，模块校验、全部测试、`go vet` 和构建通过。
- 已知风险：2026-08-24 从腾讯服务器访问 NASA NCCS 下载主机 TCP 443 失败；不得用历史归档或测试夹具冒充实时结果。
- 修正记录：提交后审查发现下载内容类型、未来时间戳筛选、目录边界和磁盘溯源恢复不足；已拒绝非 TIFF/空内容/未来或越界链接，原子保存完整来源 sidecar，并通过 `ValidTo` 支持查询时重新判断过期状态。

### P2.3 栅格处理

- 状态：已完成
- 目标：通过固定版本 GDAL CLI 对 LHASA 栅格执行中国外包范围裁剪、数值校验、概率分级、矢量化和分区统计，输出可追溯的风险快照与风险区。
- 验证：腾讯 Ubuntu 服务器执行 `sudo sh scripts/validate-go.sh`，模块校验、全部测试、`go vet` 和构建通过；固定 GDAL 3.13.3 `linux/amd64` 清单 `sha256:44fee7d4f9be0966851d7b14a0a387216897d8347f9e0ebc4e812f7217bc39d6` 运行 30 弧秒真实命令流水线通过。
- 已知风险：首版裁剪采用固定 WGS84 中国外包范围，不代表国界或行政区精确边界；P3.2 再叠加可追溯行政边界和 PostGIS 面积分析。
- 关键决策：原始文件在 GDAL 前重新核对常规文件、受控目录、大小和 SHA-256；阈值严格采用 `>0.1/>0.5/>0.9`，等级明确为 AI-GDM 派生；面积字段以 `areaCalculated=false` 表示尚未计算，禁止把零当作真实面积。

### P2.4 气象与调度

- 状态：已完成
- 目标：接入 Open-Meteo 逐小时降水与五层土壤湿度，持久化完整监测点批次；每 30 分钟执行隔离刷新，失败时只回退同点集最后成功数据。
- 验证：腾讯 Ubuntu 服务器执行 `sudo sh scripts/validate-go.sh`，全部测试、`go vet` 和构建通过；执行 `sudo sh scripts/validate-storage.sh`，PostGIS 3.5.2 天气批次事务、点集隔离和回退读取通过，Redis 返回 `PONG`；执行 `sudo sh scripts/validate-weather.sh`，外部 HTTP、Open-Meteo、采集用例、调度器和组合根竞态测试各重复 10 次通过。2026-08-24 显式在线测试从 Open-Meteo 获取北京、成都两点的 2 小时历史与 4 小时预报，逐小时降水、五层土壤湿度和来源契约通过。
- 已知风险：Open-Meteo 是数值天气模型而非地面传感器，免费接口无 SLA 且仅允许非商业用途；当前默认端点只用于本次非商业面试评估，商业或正式生产部署必须配置订阅端点和 API 密钥。超过 6 小时的最后成功数据停止参与风险计算；供应商响应单位、GMT 或数组契约漂移时系统会主动标记不可用，不会猜测字段或生成替代数据。
- 关键决策：Postgres 以六位 WGS84 点键和排序后 SHA-256 隔离监测点集，完整批次在单一事务提交；应用层校验点数、顺序、小时窗口和跨点时间轴后才保存。实时采集或保存失败时只读取同点集最后成功批次并明确标记回退；任务超时后若在一秒退出窗口内仍不响应取消，调度器终止后续执行并上报错误，避免任务重叠。外部 HTTP 网络错误在保留 `errors.Is`/`errors.As` 错误链的同时脱敏敏感查询参数，禁止 API 密钥进入调度日志。

### P2.4.1 LHASA 定时采集与回退

- 状态：已完成
- 目标：把 LHASA 发现、下载、GDAL 处理和风险分析原子保存接入独立周期任务；进程启动后立即检查，随后默认每 30 分钟检查一次；任一步骤失败时只回退未超过 12 小时的最后成功完整分析。
- 验证：腾讯 Ubuntu 服务器执行 `sudo sh scripts/validate-go.sh`，全部测试、`go vet` 和构建通过；执行 `sudo sh scripts/validate-storage.sh`，PostGIS 3.5.2 完整分析事务、进程重启读取、零风险区结果和写入失败回滚通过，Redis 返回 `PONG`；执行 `sudo sh scripts/validate-raster.sh`，固定 GDAL 真实命令流水线通过。应用测试覆盖同一制品去重、发现/下载/处理/保存失败回退以及 12 小时精确边界与超时拒绝。
- 已知风险：NASA NCCS 下载服务当前完成 TCP/TLS 后不返回 HTTP 数据，在线成功路径只能在供应商恢复后复验；不得以测试服务结果标记实时数据可用。
- 关键决策：Open-Meteo 与 LHASA 使用两个独立调度器，普通供应商错误只记录并进入下一周期；LHASA 在外部调用前预读同一模型、处理版本、供应商和数据集的最后成功完整分析，避免网络超时后丢失回退候选。快照与全部风险区在单一事务提交，并以 `analysis_complete` 排除半成品；实时刷新失败时只返回带 `fallback_last_success`、`stale=true` 的内存副本，不污染仓储原值。目录发现采用 30 秒、最多两次尝试，制品下载采用 3 分钟、最多两次尝试，共用限流器，保证供应商黑洞时仍为 GDAL 和回退保留任务预算。
- 目标提交：`fix(lhasa): 补齐定时采集与最后成功数据回退`

### P2.4.2 NASA Earthdata GIS 数据源

- 状态：已完成
- 目标：以 NASA Earthdata GIS 的公开 `LHASA_Hazard_Today/ImageServer` 替换不可达的 NCCS 目录，按中国外包范围获取真实 GeoTIFF，并继续复用 GDAL、PostGIS、定时检查和 12 小时最后成功回退。
- 验证：腾讯 Ubuntu 服务器执行 `sudo sh scripts/validate-go.sh`，模块校验、全部测试、`go vet` 和构建通过；执行 `sudo sh scripts/validate-storage.sh`，PostGIS 3.5.2 持久化测试通过且 Redis 返回 `PONG`；执行 `sudo sh scripts/validate-earthdata.sh`，全中国 `7392×4272` 目标网格以 12 个分片完成真实 HEAD、GET、前后组合修订校验和 GDAL 拼接，耗时 103.75 秒，生成 40,708,590 字节 GeoTIFF；小范围真实链路在 16.55 秒内完成发现、四片下载、拼接、风险处理并产出 40 个风险区。
- 修正记录：首次全中国验证在第 5/12 片读取响应体时触发三分钟超时，整组按原子规则失败且未提交部分制品；补充分片响应体超时和意外 EOF 的整片重试后，重新同步并完成全部 Go 与在线验证。
- 关键决策：中国外包范围固定为 WGS84 `[73.5,18,135.1,53.6]`，按 `1/120°` 目标网格生成 `4×3` 共 12 个 `2048×2048` 上限分片，边缘分片保持准确像元数；导出使用最近邻，避免双线性插值改变概率值。发现阶段持久化每片 URL、BBox、大小和强 ETag，并生成包含长度的 SHA-256 组合修订；下载前后重新 HEAD 全部清单，GET 响应必须逐片匹配 ETag 和大小，每片 SHA-256 与最终拼接 SHA-256 均进入来源记录，只有整组一致才原子保存。Earthdata 忽略错误 `If-Match`，因此条件请求只作辅助，响应 ETag 比对才是强制边界。
- 时效口径：Earthdata 未暴露精确模型运行时间或统一服务数据版本，`ObservedAt` 保持未知；新增 `RevisionFirstSeenAt` 记录组合修订首次发现时间。同一组合修订在重启后沿用首次发现时间，恰好 12 小时仍可用，超过 12 小时不得重新下载刷新时效，只能返回不可用；本地确定性分析时间写入快照 `RunAt`。
- 已知风险：供应商标注每日更新两次但无 SLA，分片之间也没有可证明属于同一次模型运行的原子版本号；系统只能通过前后 ETag 向量一致性排除采集期间变化，并明确保留这一限制。供应商持续不更新或不可达时，最后成功结果超过 12 小时即停止使用，不生成替代实时数据。
- 目标提交：`fix(lhasa): 切换至 Earthdata GIS 实时栅格服务`

### P3.1 风险引擎

- 状态：已完成
- 目标：建立不依赖供应商、数据库或传输框架的确定性领域引擎，根据风险快照阈值计算滑坡和泥石流辅助研判等级，输出数据质量置信度、触发因素、数据时效状态、规则版本和输入引用。
- 验证：腾讯 Ubuntu 服务器执行 `sudo sh scripts/validate-risk.sh`，Go 1.26.7 容器内完成风险包竞态测试 20 次和灾害领域测试 5 次；执行 `sudo sh scripts/validate-go.sh`，`go mod verify`、全仓测试、`go vet ./...` 和 `go build ./...` 全部通过。
- 关键决策：每个引擎实例固定绑定灾种、模型名和数据集，输入快照必须与能力声明完全一致，阻止把 LHASA 滑坡快照改名为泥石流结论；LHASA 概率已经来自模型计算，Open-Meteo 降雨和土壤湿度只作为上下文证据，不再次加权或抬升等级，避免重复计分；置信度只表示输入完整性、时效和质量，不等同于灾害发生概率或模型校准指标。
- 已知风险：当前 Earthdata 数据只直接支持降雨触发滑坡研判，领域引擎可处理专用泥石流概率快照，但没有专用泥石流数据时必须返回数据不足；P3.3 必须由统一注册表创建模型能力，避免调用方自行错误配置。当前评估标识使用 SHA-256 的 64 位展示摘要，若后续审计数据量显著增长应扩展摘要长度。
- 目标提交：`feat(risk): 实现滑坡泥石流风险分级引擎`

### P3.2 风险区域空间分析

- 状态：已完成
- 目标：使用 PostGIS geography 对已保存的 WGS84 风险区计算真实平方米面积和合并去重面积，建立人口、道路、POI 与行政区暴露的可审计状态合同，并把空间分析批次与区级结果独立持久化。
- 验证：腾讯 Ubuntu 服务器执行 `sudo sh scripts/validate-spatial.sh`，空间领域与应用包在竞态检测下重复 20 次通过，PostgreSQL 与 PostGIS 测试包并发完成迁移、Polygon、MultiPolygon、重叠面积去重、零风险区、非法几何、幂等写入、重启读取、并发替换一致性读取、旧结果失效和失败回滚验证；执行 `sudo sh scripts/validate-storage.sh`，全仓数据库测试、`go vet ./...`、构建、PostGIS 3.5.2 和 Redis `PONG` 通过；执行 `sudo sh scripts/validate-risk.sh` 与 `sudo sh scripts/validate-go.sh`，P3.1 专项及无数据库全仓回归通过。
- 关键决策：人口、道路、POI 和行政边界尚无已选定并导入的真实版本化图层，本阶段不得使用测试夹具或零值冒充业务结果；缺少数据集时必须返回 `unavailable` 且数量和覆盖率为 `null`。空间分析独立于基础灾害分析持久化，同一快照重新写入风险区时在事务内删除旧空间分析；数据库迁移使用事务级 advisory lock，避免多进程或测试包并发启动重复执行迁移。计算接口保持可插拔，P5.1/P4.2 接入真实数据后无需修改面积和持久化主流程。
- 已知风险：原计划把空间暴露计算排在真实人口、道路和 POI 数据接入之前，因此本阶段运行态只有面积可用；道路长度不代表道路开放，人口和 POI 的具体统计方法必须随未来数据源版本记录科学限制。
- 目标提交：`feat(spatial): 实现风险区域空间分析`

### P3.3 预警接口与多灾种扩展机制

- 状态：已完成
- 目标：提供最新风险、快照详情和显式刷新 HTTP 接口；用注册表绑定灾种、专用模型能力、读取器与刷新器，使新增灾种无需修改既有用例或 HTTP 路由。
- 验收：滑坡注册真实 Earthdata LHASA 能力；未注册灾种、非法灾种、快照与路由灾种不一致、刷新失败和过期数据均返回明确状态；接口包含来源、时效、限制、确定性研判、风险区及其面积计算标记；聚合空间分析仍由独立空间分析用例维护，不在本阶段伪装成预警响应字段；在腾讯 Ubuntu 服务器完成 API、PostGIS 和全仓验证。
- 验证：远端 `sudo sh scripts/validate-hazard.sh` 通过，应用/HTTP/组合根竞态测试重复 20 次、PostGIS 风险读取测试重复 5 次、`go vet ./...` 和 `go build ./...` 通过；远端 `sudo sh scripts/validate-go.sh` 全仓测试、校验、`go vet` 和构建通过；远端 `sudo sh scripts/validate-storage.sh` PostGIS 3.5.2 迁移、空间事务、风险快照 Repeatable Read 一致性、Redis `PONG` 和全仓回归通过。
- 关键决策：只注册具有真实专用数据源的滑坡能力；不得把 LHASA 滑坡结果改名用于泥石流。HTTP 只依赖应用服务接口，核心风险等级仍由 P3.1 确定性引擎生成，手动刷新复用 P2 已有采集链路。
- 已知风险：本阶段不做管理员令牌、CSRF 和全局限流，这些属于 P9.1；在安全阶段前，刷新接口只适合受控评估环境，部署时不得直接暴露到公网。当前只注册滑坡，其他灾种需具备真实专用数据源后再注册。
- 目标提交：`feat(hazard): 提供风险预警接口与多灾种扩展机制`

### P4.1 地图安全与坐标

- 状态：已完成
- 变更：新增 WGS84/GCJ-02 坐标转换端口与无状态适配器，覆盖中国境内、境外、边界、反向近似误差、有限数值和批量原子失败；新增高德服务端适配器，统一使用 HTTPS、超时、重试、限流、响应体上限、业务错误映射和来源摘要脱敏。
- 变更：新增 `/api/v1/map/places/nearby` 与 `/api/v1/map/routes` 受控代理，只接受固定 JSON 字段，服务端注入 `AMAP_API_KEY`/`AMAP_JSCODE`，拒绝客户端覆盖密钥、供应商地址、未知字段和超大请求体；风险和地图路由通过同一 `/api/v1` 分发器共存。
- 验证：腾讯 Ubuntu `124.220.67.163` 执行 `sudo sh scripts/validate-map.sh` 通过；坐标、配置、AMap 适配器、地图 HTTP 代理和组合根竞态测试重复 20 次通过，`go vet ./...`、`go build ./...` 通过。随后执行 `sudo sh scripts/validate-go.sh` 全仓测试通过，并执行 `sudo sh scripts/validate-hazard.sh`，P3.3 风险接口和 PostGIS 回归通过。
- 关键决策：领域与应用层始终使用 WGS84；GCJ-02 只在地图适配器边界出现。代理不开放任意 URL，不接受客户端覆盖 key/jscode；公交路线在缺少城市参数前明确返回 `invalid_request`，不伪造结果。
- 已知风险：高德 Web 服务配额、许可和接口版本由供应商控制；P4.1 只建立安全访问边界和坐标基础，避险设施、完整多方式路线和风险区排除分别在 P4.2-P4.4 实现。
- 目标提交：`feat(map): 建立地图安全代理与坐标转换`

### P4.2 避险设施

- 状态：已完成
- 目标：复用高德真实医院、应急避难场所和交通设施 POI，使用最新风险区快照对候选点执行确定性的 WGS84 点落区过滤，并在风险区数据不可用时拒绝返回未经筛选的候选点。
- 变更：新增 `application/evacuation` 设施搜索用例，先读取最新完整风险分析，再调用 `ports.PlaceFinder`；新增 WGS84 Polygon/MultiPolygon 点包含判断，支持洞环和边界保守排除；地图接口返回安全候选、排除明细、风险快照和过滤统计；PostGIS 未连接时显式返回 `503 insufficient_data`。
- 验收：应用层不依赖 HTTP/数据库具体实现；Polygon/MultiPolygon 边界、洞环、非法几何、空风险区和错误链有单元测试；真实供应商调用继续经过 HTTPS、超时、重试、限流、响应上限和来源脱敏。腾讯 Ubuntu `124.220.67.163` 使用 `sudo sh scripts/validate-shelter.sh` 通过：空间与应用竞态测试重复 20 次、AMap/HTTP/组合根测试重复 20 次、PostGIS 仓储集成重复 3 次、`go vet ./...` 和 `go build ./...` 通过；当前源码此前执行 `sudo sh scripts/validate-go.sh` 全仓测试、`go vet` 和构建通过。
- 测试命令：`sudo sh scripts/validate-shelter.sh`；回归命令：`sudo sh scripts/validate-go.sh`。
- 关键决策：设施坐标在领域和应用层统一为 WGS84，GCJ-02 只保留在高德适配器边界；点在风险区边界上按危险处理并排除；无法读取风险区或遇到非法风险几何时返回数据不可用，禁止静默返回未过滤结果；响应保留候选总数、排除数和快照来源，便于调度审计。
- 已知风险：高德 POI 分类和配额由供应商维护；风险区是最近一次完整快照的已知范围，不代表实时道路封闭或现场确认，路线可用性留到 P4.3/P4.4。
- 目标提交：`feat(shelter): 实现避险设施搜索与筛选`

### P4.3 路线规划

- 状态：已完成
- 目标：在保留驾车、步行路线端口的基础上，新增独立公交路线端口，接入高德 V5 公交规划，校验起终点数字 citycode，统一将路线几何转换为 WGS84，并保留路线来源、请求 ID、哈希和分段说明。
- 变更：新增独立 `TransitRoutePlanner` 端口；高德适配器接入 V5 `/v5/direction/transit/integrated`，解析公交、步行、铁路和出租车分段，支持数字或字符串数值字段，并将 GCJ-02 几何转换为领域 WGS84 `LineString`；地图 HTTP 代理新增 `originCity`/`destinationCity`，公交与驾车、步行走不同端口。
- 验收：HTTP 代理支持驾车、步行和带城市编码的公交请求；公交缺少城市、城市编码非数字或供应商缺少专用端口时明确返回错误，不伪造路线；路线来源、请求 ID、响应 SHA-256、分段说明和夜班限制均有契约测试。腾讯 Ubuntu `124.220.67.163` 执行 `sudo sh scripts/validate-route.sh` 通过：高德适配器、地图 HTTP、路线领域和组合根竞态测试重复 20 次，`go vet ./...` 与 `go build ./...` 通过；随后执行 `sudo sh scripts/validate-go.sh`，全仓测试、`go vet` 和构建通过。
- 关键决策：公交参数使用独立 `TransitRoutePlanner` 小端口，避免污染通用路线接口；领域层只接收 WGS84，GCJ-02 仅存在于高德适配器；路线风险区排除与排序留在 P4.4。
- 已知风险：高德公交方案字段和城市编码由供应商维护，路线可用性不等于道路开放或交管确认。
- 目标提交：`feat(route): 接入多方式疏散路线规划`

### P4.4 路线安全评分

- 状态：已完成
- 目标：读取最新完整风险区，排除几何穿越或贴合风险区的候选路线，按风险、时长、距离和标识稳定排序，并通过调度接口返回可审计的排除原因。
- 变更：新增路线安全应用用例和 WGS84 Polygon/MultiPolygon 与 LineString 相交判定；读取最新完整风险区后才调用路线供应商，排除供应商标记或几何穿越/贴合风险区的路线；安全候选按风险分数、预计时长、距离和路线标识稳定排序并生成排名，保留排除路线、风险区标识、原因和快照限制；地图 HTTP 代理支持公交专用端口与安全路线结果组合。
- 验收：空间、应用、AMap、地图 HTTP 和组合根竞态测试重复 20 次，PostGIS 仓储集成重复 3 次，`go vet ./...` 和 `go build ./...` 全部通过。腾讯 Ubuntu `124.220.67.163` 执行 `sudo sh scripts/validate-evacuation.sh` 通过；脚本覆盖 PostGIS 临时实例、路线相交/洞环/边界、风险区缺失阻断、公交端口和安全排序。
- 关键决策：风险区数据缺失、快照不一致或非法几何时拒绝返回未过滤路线；风险区相交判定只表示穿越已知风险区，不等同于道路封闭或交管确认；供应商未提供路线风险分数时保持 0 并明确限制，不由应用层臆造。
- 已知风险：路线相交只代表穿越已知风险区，不等同于道路封闭或交管确认；供应商未提供风险分数时不能自行推断。
- 目标提交：`feat(evacuation): 实现疏散路线安全评估`

### P5.1 基线数据

- 状态：已完成
- 目标：导入带来源、版本和有效期的人口、道路、资产单价及脆弱性估算系数，建立应用层只读端口和可审计的数据校验。
- 变更：新增人口/道路暴露、资产成本和脆弱性基线领域模型；增加来源、数据集版本、有效期、质量标志、转换版本和审核状态校验；新增 PostGIS 迁移、原子替换仓储、最新有效版本读取和集成测试。
- 验证：本地仅执行便携 Go 工具链 `gofmt` 与 `git diff --check`；腾讯 Ubuntu `124.220.67.163` 执行 `sudo sh scripts/validate-baseline.sh` 通过，包含 `go test -race`、PostGIS 集成、`go vet ./...` 和 `go build ./...`。
- 数据边界：无真实、版本化外部基线时返回 `insufficient_data`，不得使用硬编码金额或虚构人口。
- 目标提交：`feat(baseline): 建立损失评估基线数据`

### P5.2 损失引擎

- 状态：已完成
- 目标：基于风险区空间暴露和已验证基线，输出低、中、高人民币区间、公式版本、输入依据和置信度；缺少任一真实输入时返回 `insufficient_data`。
- 变更：新增确定性损失应用服务，按资产类别匹配成本和脆弱性基线，计算低/中/高人民币分值、影响面积、上下文暴露、输入引用和置信度；支持区域优先、`CN` 校准区域回退，并保留过期快照降级标记。
- 验证：本地仅执行便携 Go 工具链 `gofmt` 与 `git diff --check`；腾讯 Ubuntu `124.220.67.163` 执行 `sudo sh scripts/validate-loss.sh` 通过，损失领域与应用层竞态测试重复 20 次、`go mod verify`、`go vet ./...` 和 `go build ./...` 均通过。
- 本轮完成：P5.1 已完成远端验证，当前实现确定性损失计算，不允许大模型参与数值计算；错误使用 `errors.Is` 保留 `ErrInsufficientData`/`ErrNotFound` 链。
- 已知风险：基线覆盖率和脆弱性适用范围会直接影响估算置信度，结果只能作为辅助研判。
- 目标提交：`feat(loss): 实现可解释灾害损失估算`

### P5.3 损失接口

- 状态：已完成
- 目标：提供损失计算、查询和来源审计接口，统一校验请求边界、缺失数据和错误状态。
- 变更：新增严格 JSON 损失评估 HTTP 适配器，提供创建、按标识查询和来源审计接口；新增 `loss_assessments` 嵌入式迁移与 PostgreSQL 不可变评估仓储，保存公式版本、状态、快照和输入引用；来源响应删除 URL 用户信息及密钥类查询参数。
- 验证：本地仅执行便携 Go 工具链 `gofmt` 与 `git diff --check`；腾讯 Ubuntu `124.220.67.163` 执行 `sudo sh scripts/validate-loss-api.sh` 通过，损失应用、HTTP、PostgreSQL 集成测试在竞态模式下重复 10 次，`go mod verify`、`go vet ./...` 和 `go build ./...` 均通过。
- 关键决策：同一评估 ID 仅允许重复写入相同内容，不允许覆盖既有审计记录；缺少真实输入统一返回 `insufficient_data`，接口不生成示例金额；组合根只新增损失路由，未把 P6 生还回放代码混入本次提交。
- 已知风险：接口结果仍受实时快照、空间分析和基线覆盖率影响；来源审计只保存评估时的引用，不代表原始供应商文件永久留存。
- 修正记录：后续全仓回归发现内存评估仓储的标识校验和副本语义缺口，已由 `fix(loss-api): 修正内存评估仓储边界` 独立修正并推送。
- 目标提交：`feat(loss-api): 提供灾害损失评估接口`

### P6.1 历史案例

- 状态：已完成
- 目标：整理公开、真实、匿名化的失联事件案例，记录来源并明确标记历史回放。
- 变更：新增 USGS 公开滑坡/泥石流事件的匿名事件级目录，保留来源 URI、事件日期、近似中心点、公开伤亡口径和限制；为每个事件关联仅用于回放的合成搜救场景；新增稳定排序、领域校验和内存读取适配器。
- 验证：本地仅执行便携 Go 工具链 `gofmt` 与 `git diff --check`；腾讯 Ubuntu `124.220.67.163` 执行 `sudo sh scripts/validate-case.sh` 通过，历史领域、目录用例和内存适配器竞态测试重复 20 次，`go mod verify`、`go vet` 和 `go build` 均通过。
- 关键决策：目录只保存事件级匿名元数据，不保存个人身份信息；所有事件来源标记为 `historical`，回放场景明确 `Synthetic=true`，不把历史结果包装成当前人员概率。
- 已知风险：公开来源可能调整页面内容，目录更新需要人工重新核验；事件中心点和伤亡数字遵循来源口径，不能替代现场信息。
- 目标提交：`feat(case): 建立失联人员历史案例库`

### P6.2 生还评估

- 状态：已完成
- 目标：实现确定性概率区间、搜救优先级、主要因素和模型版本。
- 变更：新增匿名合成搜救场景的确定性规则评估，依据失联时长、输入完整度、环境和受困信号生成 0-100 分、宽概率区间、搜救优先级、主要因素和模型版本；应用用例只依赖场景读取端口和 UTC 时钟。
- 验证：本地仅执行便携 Go 工具链 `gofmt` 与 `git diff --check`；腾讯 Ubuntu `124.220.67.163` 执行 `sudo sh scripts/validate-survival-assessment.sh` 通过，生还领域和应用测试在竞态模式下重复 20 次，`go mod verify`、`go vet` 和 `go build` 均通过。
- 关键决策：评分与概率带由确定性规则生成，不调用大模型；结果强制标记人工复核，概率明确为未经个体校准的宽区间；分数等级和概率等级在领域校验中必须与数值一致。
- 已知风险：规则不能推断具体人员伤情或医疗结局，历史报告偏差可能导致外推失真；任何当前事件都必须由专业搜救人员重新确认。
- 目标提交：`feat(survival): 实现生还可能性辅助评估`

### P6.3 回放接口与模型卡

- 状态：已完成
- 目标：提供案例列表、评估接口及科学限制说明。
- 变更：新增生还回放 HTTP 路由，提供案例列表、案例详情、场景评估和机器可读模型卡；组合根将 `/api/v1/survival` 与风险、地图、损失路由并列挂载；响应统一携带 requestId、禁用缓存并映射领域错误。
- 验证：本地仅执行便携 Go 工具链 `gofmt` 与 `git diff --check`；腾讯 Ubuntu `124.220.67.163` 执行 `sudo sh scripts/validate-survival.sh` 通过，生还领域、应用、内存目录和 HTTP 契约测试在竞态模式下重复 20 次，`go vet` 与 `go build` 均通过。
- 关键决策：回放场景仍强制 `Synthetic=true`；模型卡明确概率未经个体校准、必须人工复核，HTTP 不提供真实人员识别字段。
- 已知风险：案例内容来自公开页面的人工核验快照，页面变更时需要重新审核；回放接口不能代表当前事件实时生还结论。
- 目标提交：`feat(replay): 提供历史案例回放与模型说明`

### P7.1 搜索证据

- 状态：已完成
- 目标：接入博查搜索，建立时效、可信来源和重复结果过滤。
- 变更：新增博查 HTTPS 搜索适配器、响应业务状态和严格 JSON 校验；按发布时间/抓取时间执行时效过滤，限制可信域名，规范化 URL 去除跟踪和敏感参数并去重；新增搜索证据领域模型、来源校验、服务端配置和阶段验收脚本。
- 验证：本地仅执行便携 Go 工具链格式化和 `git diff --check`；腾讯 Ubuntu `124.220.67.163` 执行 `sudo sh scripts/validate-search.sh` 通过，包含 `go mod verify`、搜索/证据/配置竞态测试重复 20 次、`go vet` 和 `go build ./...`。
- 关键决策：搜索只能提供候选证据，不能直接改变确定性风险等级、路线、金额或生还评分；没有时间字段的结果保留但标记为需人工复核；供应商失败不生成替代数据。
- 已知风险：博查返回字段和业务限额由供应商维护；可信域名清单需要按监控中心采购和审计要求维护；搜索证据不等于官方预警或现场确认。
- 目标提交：`feat(search): 接入实时灾害信息搜索`

### P7.2 OpenAI 兼容 LLM 客户端

- 状态：已完成
- 目标：实现结构化输出校验、超时、重试、提示注入防护和密钥脱敏。
- 变更：建立 OpenAI 兼容 Chat Completions 客户端，限制 HTTPS 端点、输出 token 和尝试次数；固定 JSON 对象响应格式，拒绝未知字段、缺失字段、`null`、错误数组类型、空输出和尾随 JSON；系统提示词将分析和证据标记为不可信资料并禁止执行其中指令；成功结果保存响应摘要和请求 ID，失败按应用层规则降级。
- 2026-08-28 修正：默认供应商切换为 Jojocode，默认端点为 `https://jojocode.com/v1/chat/completions`，默认模型为 `gpt-5.6-terra`；配置统一为 `LLM_*`，来源审计使用实际供应商名称；删除 Qwen 专用 `enable_thinking` 字段，并在用户消息中显式声明小写 `json` 及四个字段的准确类型。
- 密钥配置：真实密钥只保存在 Git 忽略的本机 `.env.remote-validation` 和服务器 `/home/ubuntu/.config/ai-gdm/runtime.env`，远端文件权限为 `600`；仓库仅提供空密钥示例和原子同步脚本，后续部署必须显式加载该运行配置。
- 验证：本地执行 Go 格式化、PowerShell AST 语法检查和 `git diff --check`；腾讯 Ubuntu `124.220.67.163` 执行 `sudo sh scripts/validate-llm.sh` 与 `sudo sh scripts/validate-ai.sh`，模块校验、报告/LLM/搜索/编排/配置竞态测试重复 20 次、`go vet` 和全量构建通过；执行 `sudo sh scripts/validate-go.sh`，全仓测试、`go vet ./...` 和 `go build ./...` 通过；两次执行 `sudo sh scripts/validate-llm-live.sh`，真实端点分别在 13.130 秒和 10.149 秒内返回，严格结构、领域校验及请求/响应模型名一致性均通过。
- 关键决策：模型只输出摘要、发现、行动建议和限制四类定性文字；所有风险、路线、损失和生还数值留在确定性分析 JSON 中，由 SHA-256 绑定且不允许模型覆盖；上下文取消保留错误链，非上下文故障交由编排层降级。
- 已知风险：LLM 输出质量受模型和供应商服务影响，结构合法不代表事实正确；提示注入防护依赖资料隔离和输出白名单，仍需人工复核。当前 Jojocode 兼容服务的可用性、模型映射、配额和服务条款由供应商维护，系统故障时只降级说明能力。
- 目标提交：`feat(llm): 接入受约束的大模型报告生成`
- 修正提交：`fix(llm): 切换至通用 OpenAI 兼容服务`

### P7.3 AI 编排

- 状态：已完成
- 目标：组合确定性数值、搜索证据和中文报告；禁止大模型修改风险等级、路线及金额。
- 变更：新增智能研判应用编排服务，计算并校验确定性分析 SHA-256，收集带来源搜索证据，调用解释性 LLM 客户端，并在任一可选供应商失败时保留确定性结果和限制说明；新增 `/api/v1/ai/report` 严格 JSON HTTP 适配器，拒绝未知字段、尾随 JSON 和超大请求体。
- 验证：本地仅执行便携 Go 工具链格式化和 `git diff --check`；腾讯 Ubuntu `124.220.67.163` 执行 `sudo sh scripts/validate-ai.sh` 通过，搜索/LLM/编排/HTTP/配置竞态测试重复 20 次、`go mod verify`、`go vet ./...` 和 `go build ./...` 全部通过；首次验证发现脚本缺少 Go 模块代理，已补充 `GOPROXY` 后复验通过。2026-08-28 在通用适配器和真实 Jojocode 配置下再次完成回归。
- 关键决策：模型输入只包含去标识化分析 JSON、不可变字段和已筛选证据；响应校验分析摘要、证据可用标志和说明可用标志的一致性；模型或搜索故障不生成替代风险、路线、损失或生还数字。
- 已知风险：HTTP 请求的 `analysis` 仍需由上游确定性用例产生，接口本身不能证明调用方没有篡改业务语义；外部证据和模型说明必须由值班人员复核。
- 目标提交：`feat(agent): 建立可审计的智能研判流程`

### P8.1 控制台框架

- 状态：已完成
- 目标：使用 Go 模板构建中文控制台、数据源状态和更新时间展示。
- 变更：新增独立 `dashboard` 应用服务，从配置、PostGIS 最后成功 LHASA 风险分析和同点集 Open-Meteo 批次汇总只读状态；新增嵌入式 Go 模板和响应式工业化中文控制台，展示运行环境、版本、来源、最后成功时间、有效期和降级状态；根路由最后挂载且不遮蔽健康探针与 `/api/v1`。
- 验证：本地执行 Go 格式化和 `git diff --check`；腾讯 Ubuntu `124.220.67.163` 执行 `sudo sh scripts/validate-web.sh`，控制台应用、模板适配器和组合根竞态测试重复 20 次、`go vet ./...` 和全量构建通过；执行 `sudo sh scripts/validate-go.sh`，全仓测试、`go vet` 和构建通过。
- 关键决策：页面请求只读取配置和持久化数据，不触发 NASA、Open-Meteo、高德、博查或 LLM 调用；高德、搜索和 LLM 尚无调用状态仓储时只显示已配置或未启用，不伪造更新时间；无数据库或无记录时页面仍返回 200 并明确显示不可用或等待数据。
- 已知风险：当前供应商最后尝试、失败原因和实时指标尚未统一持久化，P9.2 再补充观测注册表；风险地图、疏散和评估工作区由 P8.2-P8.4 逐步接入。
- 目标提交：`feat(web): 建立灾害监控控制台`

### P8.2 风险地图

- 状态：已完成
- 目标：展示风险区、热点、等级、来源和数据过期状态。
- 变更：内嵌固定版本 Leaflet 1.9.4 及许可证，消费 `/api/v1/hazards/landslide/risks/latest` 的 WGS84 Polygon/MultiPolygon；按等级着色并展示概率、面积、模型、来源、规则、获取时间、有效期、置信度和限制；浏览器最多绘制 3000 个风险区并明确显示实际数量与总数；404、503、超时、非 JSON、零风险区和过期数据均使用独立真实状态，不生成演示图层。
- 验证：本地执行 Go 格式化、`node --check` 和 `git diff --check`；腾讯 Ubuntu `124.220.67.163` 执行 `sudo sh scripts/validate-map-ui.sh`，Web 适配器与组合根竞态测试重复 20 次、全仓 `go vet/build` 和 JavaScript 语法检查通过；再执行 `sudo sh scripts/validate-go.sh`，全仓测试、`go vet` 和构建通过。
- 关键决策：风险几何保持 WGS84 并直接交给 Leaflet，不向浏览器下发高德服务端密钥；所有供应商文本通过 DOM `textContent` 渲染；地图只重新读取持久化的最后成功分析，不提供未鉴权的手动采集入口。
- 已知风险：浏览器绘制上限会优先展示最高风险区域，不代表被截断区域风险为零；公网瓦片服务不可用时底图可能缺失，但风险 API 和来源详情仍可查询；真实浏览器多视口与 API 场景矩阵在 P8.4/P9.3 完成。
- 目标提交：`feat(map-ui): 实现实时风险地图交互`

### P8.2.1 Docker 验证所有权兼容

- 状态：已完成
- 目标：让普通用户拥有的干净克隆可直接通过 `sudo sh scripts/validate-*.sh` 运行容器化 Go 验证，不依赖宿主机 Git 全局配置。
- 变更：新增统一验证容器包装脚本，通过进程级 `GIT_CONFIG_*` 将容器内只读源码挂载点 `/src` 声明为可信；全部既有 Docker 验证脚本改由该入口启动临时容器；新增普通用户克隆和 `sudo` 执行的所有权回归脚本。
- 验证：腾讯 Ubuntu `124.220.67.163` 对全部 Shell 脚本执行 `sh -n`；把当前源码复制到 UID 1000 的普通用户目录后执行 `sudo sh scripts/validate-map-ui.sh`，Web 竞态测试、`go vet`、`go build` 和 JavaScript 语法检查通过，未修改宿主机 Git 配置；推送后再由 `validate-worktree-ownership.sh` 对远端干净克隆复验。
- 关键决策：不使用 `git config --global --add safe.directory`，避免污染评估机；包装脚本同时覆盖 Go、Node 和 GDAL 验证容器，后续阶段默认复用同一入口。
- 已知风险：回归依赖执行用户具备免交互 `sudo` 和 Docker 权限；Windows Docker Desktop 场景由 P10.3 单独验证。
- 目标提交：`fix(validation): 兼容非 root 工作区的容器校验`

### P8.2.2 风险地图时效与负载边界

- 状态：已完成
- 目标：修复独立 QA 发现的有效期 fail-open、刷新失败后旧元数据残留，以及浏览器在完整读取大响应后才截断的问题。
- 变更：新增专用地图投影接口，在服务端按风险优先级排序并限制源风险区数、可见风险区数、单区与总顶点数、几何字节和响应字节；严格校验 WGS84 面几何并返回闭合的省略计数契约。浏览器使用流式有界响应读取，缺失或非法有效期、计数或几何契约异常均按不可用处理；跨过 `validTo` 后自动同步更新主状态、数据状态和详情；刷新失败时清空旧图层与元数据并恢复全国视图；最后成功回退使用独立降级状态。
- 验证：本地使用便携 Go 工具链格式化，`node --check` 和 `git diff --check` 通过；腾讯 Ubuntu `124.220.67.163` 执行 `sudo sh scripts/validate-map-ui.sh`，地图 API、Web 适配器和组合根竞态测试重复 20 次、`go vet`、全量构建及 JavaScript 语法检查通过；执行 `sudo sh scripts/validate-risk-map-browser.sh`，真实 Chromium 覆盖非法有效期、短有效期自动过期、成功后 503、成功后超时、超量风险区和复杂几何，结果为 `7 passed (8.9s)`。
- 关键决策：完整审计接口保持原契约，浏览器只消费有硬上限的地图投影；任何不完整或损坏契约均 fail-closed，不允许用旧值或模拟值冒充当前风险；被省略区域仍计入总数并明确提示不代表风险为零。
- 后续修复：P8.2.3 已把十万个风险区上限下沉为 PostGIS 前置 `COUNT(*)` 与风险优先级 `LIMIT` 查询；超过上限时不读取风险区明细。
- 目标提交：`fix(map-ui): 修复风险图层时效与负载边界`

### P8.2.3 风险地图生产契约边界

- 状态：已完成
- 目标：关闭独立回归新增的权威有效期绕过、全省略误报、完整风险区预加载、fallback 状态不可达，以及未指纹化静态脚本缓存问题。
- 变更：风险地图新增专用读取 port，PostGIS 在同一只读一致性事务中先计数，超过十万个风险区立即停止且不读取明细；未超限时按风险优先级带 `LIMIT` 读取。浏览器要求快照与来源有效期均为严格 UTC RFC3339 字符串、日历有效且完全一致，并区分全部省略、fallback、陈旧和过期状态。
- 缓存：未指纹化 JavaScript 和 CSS 改为 `Cache-Control: no-cache`，部署后浏览器必须重新校验脚本，不再继续使用最长一小时的旧缺陷版本。
- 验证：精确暂存树 `85b9dc4621e07dea124e3f76d4dbb667df9d46d7` 在腾讯 Ubuntu 执行 `sudo sh scripts/validate-map-ui.sh`、`sudo sh scripts/validate-risk-map-browser.sh` 和 `sudo sh scripts/validate-go.sh` 全部通过；Chromium 16 个场景全部通过。
- 已知风险：单一快照超过十万个风险区时地图接口按数据不足返回，不会提高内存上限或提供不完整研判；后续规模化方案应采用矢量瓦片或分区查询。
- 目标提交：`fix(map-ui): 收紧风险地图生产契约边界`

### P8.2.4 全省略图层到期状态

- 状态：已完成
- 目标：修复“全部风险区被省略”结果跨过有效期后被错误改写成正在展示最后成功图层的组合状态冲突。
- 变更：到期回调重新进入数据形态渲染分支；有图层时显示过期复核，无图层且全部省略时保持 `unavailable`，并同时显示数据已过期、风险区全部省略和不得降低风险判断。
- 验证：精确暂存树 `4748a07237dbcf63b660b5ba989d67ae6008e917` 在腾讯 Ubuntu 执行地图专项、Chromium 和 Go 全量验证通过；Chromium 17 个场景包含 `all_zones_omitted_then_expiry` 假时钟组合回归。
- 已知风险：无；该状态只表示地图负载不可用，完整风险记录仍需通过审计接口复核。
- 目标提交：`fix(map-ui): 修复全省略图层到期状态`

### P8.3 疏散工作台

- 状态：已完成
- 目标：完成起点选择、交通方式、候选路线和风险警告界面。
- 变更：新增 WGS84 起终点输入、避险设施检索、候选终点设置、驾车/步行/公交切换、公交城市编码、多路线规划、风险排除明细和移动/桌面响应式界面；浏览器只调用服务端地图代理，不接触高德密钥。
- 契约边界：严格校验快照与来源有效期、fallback/陈旧/过期状态、响应字节、设施/路线数量、路线顶点与步骤；供应商坏数据统一映射为 `503 unsafe_provider_result`，用户输入错误保持 `400 invalid_request`；所有候选被风险区排除或被安全上限全部省略时按不可用处理，不显示绿色可调度状态。
- 验证：精确暂存树 `5df53187e9e45c515ac101c0552e7f2d22ac3dd9` 在腾讯 Ubuntu `124.220.67.163` 完成 `validate-evacuation-ui.sh`、风险地图 Chromium `17/17` 和 `validate-go.sh`；开发侧在独占端口 `28411/28412/28413` 连续三轮疏散工作台真实 Chromium 均为 `42/42`。独立 QA 对同一 tree 再完成疏散专项、连续三轮 `42/42`、`validate-map-ui.sh`、风险地图 `17/17` 和 Go 全量回归。最终提交除本进度账本外不得包含未验证源码差异。
- 关键决策：交通方式测试点击用户实际可见的标签控件并断言隐藏 radio 状态，不使用 `force:true`；短有效期夹具使用 10 分钟确定性窗口并由 Playwright 假时钟推进 600.1 秒，避免把真实页面加载耗时当成业务过期；wire body 通过页面内 `fetch` 校验，不依赖易漂移的 CDP 响应体读取。
- 已知风险：供应商返回候选路线只表示地图服务可返回且路线未穿越当前已知风险区，不代表交管部门确认道路开放；浏览器回归使用真实 HTTP/应用服务和 typed provider stub，真实高德公网可用性仍需在系统级与部署验收中复核。
- 目标提交：`feat(evacuation-ui): 实现疏散调度工作台`

### P8.4 评估界面

- 状态：已完成
- 目标：完成损失、生还历史回放、AI 报告和来源面板，并进行响应式检查。
- 变更：完成损失评估、公开历史案例与合成匿名场景回放、服务端 Authority 引用式 AI 报告、来源审计和模型卡界面；损失输入由服务端绑定风险快照、空间暴露投影和已批准基线，金额使用十进制字符串传输；AI 仅接收服务端解析的版本化确定性投影，浏览器不能自报权威数值，搜索证据经过来源最小化、隐私过滤、请求级数量和响应预算限制。
- 数据链：新增 WorldPop、Overpass 和 geoBoundaries 暴露采集与 PostgreSQL 不可变投影，统一 UTC 微秒时间、来源摘要、空间分析身份和重复写入语义；供应商坏数据、过期数据、证据缺失或越界均 fail-closed，不用硬编码或浏览器夹具冒充生产数据。
- 验证：已验收源码 tree 为 `06897189acfad9b728b8ba5942a016dd75c3bb7b`，独立归档 SHA-256 为 `9e83b8b433e7544ff405da2d0367dae51a3a80dcd3430fa2868b1f1f1584bf8a`，314 个受审计源码文件摘要为 `7333a8747dfd4e727ff80b2ced6a8b6904dfabdfcbe90957bdf47e6f3856692d`。本地 `go test ./...`、`go vet ./...` 和 `go build ./...` 通过；腾讯 Ubuntu 完成 Agent 竞态与分配探针 20 轮、`validate-assessment-ui.sh`、连续三轮评估 Chromium `119/119`、风险地图 `17/17`、疏散 `42/42`、疏散 PostgreSQL/PostGIS 专项和 `validate-go.sh` 全量验证，均无失败或跳过，验证容器和网络已回收。
- 关键决策：生还功能只允许“公开历史案例 + 合成匿名场景”回放并强制人工复核；损失、风险、路线和生还评分由确定性代码生成，LLM narrative 与确定性卡片隔离；Authority 使用规范封包 SHA-256 绑定，`resolvedAt` 不参与摘要；应用层不信任 Searcher 的数量承诺，按规范化后的请求 `EvidenceLimit` 和全局 20 条双重限界后才进入 LLM。
- 已知风险：损失结果仍依赖已导入、已批准且在有效期内的真实基线和实时暴露投影；WorldPop、Overpass、geoBoundaries、博查及 LLM 公网可用性需要 P9.2 持续观测和降级告警；本阶段关闭的是 P8.4 代码 QA，不代表 P9/P10 安全、系统测试、部署与正式发布验收完成。
- 目标提交：`feat(assessment-ui): 完成智能评估与报告界面`

### P9.1 安全加固

- 状态：已完成
- 目标：增加管理员令牌、CSRF、限流、安全响应头、输入校验和密钥审计，默认拒绝未授权刷新、跨站写请求、超量请求和不安全密钥配置。
- 变更：全局拦截 `/api/v1` 的非安全方法，统一执行 Bearer 操作员令牌、固定 CSRF 头、同源 Origin/Fetch Metadata、严格 JSON 媒体类型、1 MiB 请求预算和有界分级限流；增加 CSP、点击劫持、MIME 嗅探、引用与浏览器权限防护，并为 HTTP 服务器固定请求头、读写和空闲超时。
- 密钥边界：生产模式必须配置 `APP_ADMIN_TOKEN`；配置层拒绝占位值、控制字符、HTTP 头空白、过短令牌和跨用途密钥复用。浏览器令牌只保存于 JavaScript 闭包内存，不写入 Cookie、HTML、`localStorage` 或 `sessionStorage`；401 会立即清除。
- 输入边界：修复地图 API 对 chunked 超量正文的截断绕过，所有安全错误返回 `no-store` 中文 JSON 和规范 Request-ID；限流仅使用直连 `RemoteAddr`，不信任客户端 `X-Forwarded-For`，且客户端分桶缓存最多保留 4096 项。
- 验证：最终验收源码树为 `5d11b18647b70df4accf8319a8661c7f21cb2f5c`，安全门禁 source SHA-256 为 `4d90e60f8f3b0cf59c9a9c55bca9f6c05f7a1c6bf96f1e0a7c6ea355f6125b4b`。腾讯 Ubuntu 执行 `validate-security.sh`，安全目标包竞态测试 20 轮、暂存树密钥审计、`go vet ./...`、`go build ./...` 和安全 Chromium `5/5` 全部通过；同一树继续通过风险地图 Chromium `17/17`、疏散 UI/PostGIS 与 Chromium `42/42`、评估专项与 Chromium `119/119`（`failed=0`、`skipped=0`，source SHA-256 `02eafb1054647a157743089d28682be92b83b69112bc0c71ae238ce0ae5bb17e`）以及离线全仓 `validate-go.sh`。验证结束后没有遗留 `ai-gdm-*` 容器或网络。
- 独立审查：覆盖无脚本令牌泄露、严格 JSON/Origin、公开路径与非法 Request-ID 共享限流、YAML/暂存 blob 私钥扫描、浏览器结果审计、信号中断资源回收和弱管理员令牌派生模式；随机 32 字节十六进制及 48 字节 Base64URL 强令牌样本均可正常加载，错误信息不回显令牌。
- 已知风险：MVP 仍使用单一共享操作员令牌，尚不具备个人账号、角色和令牌操作人归属；P9.2 需要补齐操作审计、供应商运行状态、依赖就绪检查和指标，细粒度 RBAC 留待后续产品化。
- 目标提交：`feat(security): 加固评估服务安全边界`

### P9.2 可观测与降级

- 状态：已完成
- 目标：增加有界指标、受保护写操作审计、供应商与调度任务运行状态、过期提示和无实时事件源状态。
- 变更：新增固定组件观测端口和进程内注册表，覆盖 LHASA、Open-Meteo、高德、博查与 LLM 的成功、降级和失败；提供受管理员 Bearer 保护的精确 `GET/HEAD /metrics`；为受保护写请求记录固定动作、规范 Request-ID、状态和耗时；控制台展示最近尝试、最近成功、过期、最后成功回退和实时事件源未知状态。
- 数据口径：指标仅表示本进程启动后的业务级观测；HTTP 成功、缓存命中和已配置状态均不得冒充供应商实时可用。博查证据被全部过滤或 LLM 输出被业务校验降级时记录为 `degraded`，不记为成功。
- 安全边界：指标标签使用固定组件标识，不记录请求体、查询串、令牌、Cookie、原始错误、人员信息或动态业务 ID；共享管理员令牌只能证明角色授权，不能归属到具体人员。写处理器 panic 仍返回 500 并产生一条有界审计。
- 验证：固定源码树 `7cd00f6612881aaccebfcb9f23c058da7b4c488b`、归档 SHA-256 `23b08e7aca48ef3f374230808e0db0480c000f4fad4ea5d28af1f006ab0a33d7`、source SHA-256 `549244671fb6dec896af99e398caee8811941723ecc16bcb7d788deb8d8a67bf` 在腾讯 Ubuntu 重建一致；`validate-observability.sh` 的竞态测试 20 轮、`go vet/build` 通过；风险地图 Chromium `17/17`、疏散 UI/PostGIS 与 Chromium `42/42`、评估 Chromium `119/119`、离线全仓 `validate-go.sh` 均通过；结束后无 `ai-gdm-*` 容器或网络。
- 已知风险：进程内观测在重启后归零，不等同于长期 SLA；当前没有经核验的实时事件目录，页面显示未知。2026-08-30 首次高德探针曾返回 `INVALID_USER_KEY`；随后控制台截图确认 Key 的服务平台为“Web服务”且没有安全密钥，P9.3 以仅 `AMAP_API_KEY` 的服务端配置重试后，真实医院周边搜索和步行路线已通过。系统在供应商不可用时仍必须降级，不伪造路线或设施结果。按用户当前优先级，本阶段未重复执行完整 P9.1 安全浏览器门禁，P9.1 已有独立验收记录。
- 目标提交：`feat(observability): 完善监控审计与服务降级`

### P9.3 完整测试

- 状态：已完成
- 目标：把观测、真实暴露数据、风险地图、疏散、损失、生还回放、AI 报告与全量 Go 构建收敛为一个可重复的 MVP 系统门禁。
- 固定性：门禁必须从暂存 tree 创建只读快照，并将所有子门禁结果绑定到同一 tree 和 source SHA-256；缺项、重复、失败、跳过或摘要漂移均 fail-closed。
- 执行范围：包含 PostGIS/暴露供应商专项、风险地图 Chromium、疏散 PostGIS 与 Chromium、评估 Chromium 和离线全量 Go；按用户 2026-08-30 的优先级，不重复 P9.1 安全浏览器专项。
- 实时预检：腾讯 Ubuntu 已通过 NASA Earthdata 中国区域与处理链、Open-Meteo 降雨/土壤湿度在线契约；Jojocode 模型目录鉴权成功并包含 `gpt-5.6-terra`，但生产同形 Chat 请求当前明确返回 `upstream_error`，系统门禁仅允许记录受审计降级，并验证确定性 Authority 仍可返回，不能表述为真实报告生成成功。高德控制台已确认 Web 服务 Key 类型，服务端适配器只强制 `AMAP_API_KEY`、可选透传 `AMAP_JSCODE`；真实 `/v5/place/around` 医院周边搜索与 `/v5/direction/walking` 步行路线已通过，并根据实际 V5 响应补齐 `cost.duration` 和 `step_distance` 解析。
- 固定验收：提交前源码树为 `dd913287ebec0af098e1333189e2d7cf0621a616`，独立归档 SHA-256 为 `0f563f734fb62da64f0506b96c5f3885708dc8031528614645bfd93f599e470d`，腾讯 Ubuntu 以 `LC_ALL=C` 重算的系统门禁 source SHA-256 为 `c021177e6450d0197f463ef173c1453b2436c9612556f0cc89f40abeb971eb6c`，归档重建 tree 与本地暂存 tree 完全一致。
- 完整验证：第二次完整系统门禁通过并由结果审计器确认 `15/15`；风险地图 Chromium `17/17`、疏散 Chromium `42/42`、评估 Chromium `119/119`，PostGIS 暴露投影与损失链、疏散 PostGIS 链、离线全仓 Go 测试、竞态测试、`go vet` 和完整构建均通过。第一次完整运行仅因 geoBoundaries 固定几何下载瞬时超时而失败，门禁正确返回非零；第二次 WorldPop、Overpass、geoBoundaries 与数据库全链全部通过。
- 实时验证：NASA Earthdata 中国区域获取 `12` 个分片、`40446886` 字节并完成处理链；Open-Meteo 降雨与土壤湿度在线契约通过；高德 Web 服务 Key 在服务端完成医院周边候选搜索和步行路线调用，未使用或下发 Web 端 JS Key/安全密钥；Jojocode 模型目录鉴权和模型匹配通过，但最终验收运行的 Chat 请求返回明确 `upstream_error`，系统按合同以 `llm_outcome=degraded` 保留确定性 Authority，并验证无供应商时的降级报告结构。
- 资源回收：系统门禁结束后，腾讯 Ubuntu 无遗留 `ai-gdm-*` 容器或 Docker 网络；密钥继续只保存在服务器权限为 `600` 的私有环境文件中，未进入源码、日志或 Git 暂存树。
- 已知风险：高德 Web 服务返回的只是候选设施和候选路线，不等同于交管部门确认道路开放；真实调用仍受高德配额、服务启用和供应商可用性影响。外部 LLM 在最终验收运行中处于上游降级状态，MVP 会保留确定性结果并明确标记解释性报告不可用，不伪造调度结果或 AI 说明。
- 目标提交：`test(system): 完成系统级自动化验证`

### P10.1 容器部署

- 状态：已完成
- 目标：建立可在腾讯 Ubuntu 上运行的生产应用镜像、PostGIS/Redis Compose 编排和固定源码树容器验收入口。
- 变更：新增固定 Go 1.26.7 与 GDAL 3.13.3 摘要的多阶段 `Dockerfile`、非 root 健康检查二进制、只读根文件系统、内部数据库网络、持久化卷、资源限制、私有运行配置模板和部署说明；PostgreSQL、Redis 不映射宿主机端口，应用容器使用 UID/GID `10001:10001`。
- 固定验收：提交前源码 tree 为 `027647501ddf620c4c49fb95d204c2c4d4bc3b1f`，Git archive SHA-256 为 `97385e6915cb1621f397914242260824537064d060b1b998b8346bb5f1397e55`，受审计 source SHA-256 为 `7ad79238640dfafe8478978a051c7dc6fa10be300fae872b5f0931b09fc97af9`；腾讯 Ubuntu 重建 tree 完全一致。
- 验证：`validate-docker.sh` 在独占 Compose project 中完成应用镜像构建、三服务 healthy、HTTP 存活/就绪/控制台、非 root、只读根、GDAL 3.13.3、PostGIS 3.5、9 个迁移、Redis、PostgreSQL/Redis/LHASA 重启持久化、日志密钥检查和零遗留资源验证；同一远端 Git 物化树继续通过 `validate-go.sh` 全仓测试、`go vet` 和完整构建。
- 关键决策：构建代理作为显式参数提供，运行时密钥只写入权限受限且被 Git 忽略的 env 文件；容器门禁从暂存 tree 创建只读快照并以 tree/source SHA 绑定结果，失败路径只按本轮标签和 Compose project 回收资源。
- 已知风险：当前直接暴露 HTTP `8080`，尚无 TLS 终止；实时供应商仍要求服务器联网；仓库不内置资产价格和脆弱性数值，未导入真实、已批准且可审计的损失基线时损失接口返回 `insufficient_data`，不得用测试数据冒充生产金额。
- 目标提交：`build(docker): 完成生产容器与编排配置`

## 关键决策与风险

- 数值结论由确定性分析引擎生成；大模型只能解释和汇总，不能修改风险等级、路线、金额或生还评分。
- 无实时数据时显示过期或不可用状态，不生成模拟数据冒充实时数据。
- 失联人员模块仅使用有来源的匿名历史案例，并明确显示“历史案例回放”。
- 高德交通结果表示路线服务可返回且未穿越已知风险区，不等同于交管部门确认道路开放。
- 发布包通过 GitHub Release 交付，Git 历史只保存源码、测试、部署定义和文档。

## 恢复步骤

1. 阅读本文件的“当前快照”“总体清单”和最后一条阶段记录。
2. 运行 `git status --short --branch`，确认是否存在未提交工作。
3. 运行 `git rev-parse HEAD` 和 `git ls-remote origin refs/heads/main`，确认本地与远端是否同步。
4. 运行最近阶段记录的测试命令；只有通过后才继续。
5. 将目标阶段标记为“进行中”，写明本轮目标、验证和风险，再开始编码。

## 阶段完成门槛

1. 当前阶段功能和阶段内测试完成。
2. `go build ./...`、`go test ./...`、`go vet ./...` 通过；尚未引入 Go 时执行对应的仓库检查。
3. 检查 `git diff` 和 `git diff --cached`，确认只包含本阶段文件。
4. 搜索令牌、密码、私钥、Cookie、Authorization 和真实 `.env`，确认没有敏感信息进入暂存区。
5. 使用计划中的中文 Conventional Commit 信息提交并推送 `origin/main`。
6. 校验本地 HEAD 与远端 `main` SHA 一致，随后进入下一阶段。
