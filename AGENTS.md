# AI-GDM 项目指令

## 通用要求

- 所有文本文件使用 UTF-8；面向用户的文案、日志说明和文档使用中文。
- 每次恢复工作先阅读 `PROGRESS.md`，检查本地与 `origin/main` 是否同步。
- 每个小阶段只能提交当前阶段范围；提交前必须完成测试、暂存差异检查和敏感信息扫描。
- 提交信息使用 `<type>(<scope>): <中文摘要>`；已推送历史禁止强推和改写。
- API 密钥、令牌、Cookie、真实人员数据、原始数据、运行数据和发布产物不得进入 Git。
- 容器、数据库、缓存、完整构建和浏览器验收在指定腾讯 Ubuntu 验证服务器执行；连接参数保存在被忽略的 `.env.remote-validation`。
- 本地仅执行源码编辑、格式化和快速静态检查，不在本地启动项目验证依赖。

## Go 代码约束

- 领域层和应用层不得依赖 HTTP SDK、数据库驱动、缓存客户端、模板或具体供应商类型。
- 外部接口采用中文注释；接口由使用方定义并保持小粒度，禁止万能 Provider 接口。
- 单个函数不超过 50 个非空、非注释代码行，嵌套层级不超过 3 层。
- 错误必须保留上下文并支持 `errors.Is`/`errors.As`；禁止通过字符串匹配判断错误类型。
- 时间统一以 UTC 存储和交换，页面层再转换为用户时区。
- 空间数据库统一保存 WGS84；高德 GCJ-02 转换只允许发生在地图适配器边界。
- 核心数值由确定性代码计算；大模型只负责解释，不得修改风险、路线、损失和搜救评分。

## 目录与依赖

- `internal/domain`：实体、值对象、领域错误和纯规则。
- `internal/application`：用例编排，只依赖领域模型和 ports。
- `internal/ports`：由应用层使用方定义的小接口。
- `internal/adapters`：HTTP、数据库、缓存、GDAL、地图和 AI 的具体实现。
- `internal/platform`：配置、日志、HTTP 服务器和观测基础设施。
- `cmd`：组合根，只负责装配依赖和进程生命周期。
- `web`：模板、样式和浏览器端最少量脚本。

## CodeGraph

- 结构问题优先使用 CodeGraph：先 `codegraph_context`，需要源码时再使用一次 `codegraph_explore`。
- 字面量、日志文本和配置值才使用 `rg`；不要用 grep 重复验证 CodeGraph 的 AST 结果。
- 写入文件后等待索引同步，不要立即查询刚修改的符号。
